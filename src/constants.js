'use strict';

/**
 * Shared constants used across multiple server modules.
 */

/** Canonical column identifiers for the Kanban board. */
const COLUMNS = ['todo', 'in-progress', 'done'];

module.exports = { COLUMNS };
